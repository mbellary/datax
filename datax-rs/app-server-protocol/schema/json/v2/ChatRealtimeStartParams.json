{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ChatRealtimeStartTransport": {
      "description": "EXPERIMENTAL - transport used by thread realtime.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "websocket"
              ],
              "title": "WebsocketChatRealtimeStartTransportType",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "title": "WebsocketChatRealtimeStartTransport",
          "type": "object"
        },
        {
          "properties": {
            "sdp": {
              "description": "SDP offer generated by a WebRTC RTCPeerConnection after configuring audio and the realtime events data channel.",
              "type": "string"
            },
            "type": {
              "enum": [
                "webrtc"
              ],
              "title": "WebrtcChatRealtimeStartTransportType",
              "type": "string"
            }
          },
          "required": [
            "sdp",
            "type"
          ],
          "title": "WebrtcChatRealtimeStartTransport",
          "type": "object"
        }
      ]
    },
    "RealtimeConversationVersion": {
      "enum": [
        "v1",
        "v2"
      ],
      "type": "string"
    },
    "RealtimeOutputModality": {
      "enum": [
        "text",
        "audio"
      ],
      "type": "string"
    },
    "RealtimeVoice": {
      "enum": [
        "alloy",
        "arbor",
        "ash",
        "ballad",
        "breeze",
        "cedar",
        "coral",
        "cove",
        "echo",
        "ember",
        "juniper",
        "maple",
        "marin",
        "sage",
        "shimmer",
        "sol",
        "spruce",
        "vale",
        "verse"
      ],
      "type": "string"
    }
  },
  "description": "EXPERIMENTAL - start a thread-scoped realtime session.",
  "properties": {
    "chatId": {
      "type": "string"
    },
    "clientManagedHandoffs": {
      "description": "Leaves Codex response handoffs to the client's explicit append calls instead of forwarding them automatically. Defaults to false.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "codexResponseHandoffPrefix": {
      "description": "Optional prefix added to automatic V1 Codex commentary sent with `conversation.handoff.append` when `codexResponsesAsMessages` is not true. Final answers are sent without the prefix.",
      "type": [
        "string",
        "null"
      ]
    },
    "codexResponseMessagePrefix": {
      "description": "Optional prefix added to automatic Codex response messages when `codexResponsesAsMessages` is true.",
      "type": [
        "string",
        "null"
      ]
    },
    "codexResponsesAsMessages": {
      "description": "Sends automatic Codex responses as realtime conversation messages instead of handoff appends.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "includeStartupContext": {
      "description": "Set to false to start without Codex's startup context. Omitted or null includes it.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "model": {
      "description": "Overrides the configured realtime model for this session only.",
      "type": [
        "string",
        "null"
      ]
    },
    "outputModality": {
      "allOf": [
        {
          "$ref": "#/definitions/RealtimeOutputModality"
        }
      ],
      "description": "Selects text or audio output for the realtime session. Transport and voice stay independent so clients can choose how they connect separately from what the model emits."
    },
    "prompt": {
      "type": [
        "string",
        "null"
      ]
    },
    "realtimeSessionId": {
      "type": [
        "string",
        "null"
      ]
    },
    "transport": {
      "anyOf": [
        {
          "$ref": "#/definitions/ChatRealtimeStartTransport"
        },
        {
          "type": "null"
        }
      ]
    },
    "version": {
      "anyOf": [
        {
          "$ref": "#/definitions/RealtimeConversationVersion"
        },
        {
          "type": "null"
        }
      ],
      "description": "Overrides the configured realtime protocol version for this session only."
    },
    "voice": {
      "anyOf": [
        {
          "$ref": "#/definitions/RealtimeVoice"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "chatId",
    "outputModality"
  ],
  "title": "ChatRealtimeStartParams",
  "type": "object"
}