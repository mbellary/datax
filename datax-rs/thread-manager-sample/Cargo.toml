[package]
name = "datax-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Codex workspace dependency.
# Add new Codex surface area to `datax-core-api` instead of depending on
# additional `datax-*` crates here.
datax-core-api = { workspace = true }
tracing = { workspace = true }
